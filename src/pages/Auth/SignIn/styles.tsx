import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  card: {
    width: '100%',
  },
  col: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'center',
  },
});
